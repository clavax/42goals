<table width="100%" align="center" style="border-collapse:collapse;" cellpadding="0" cellspacing="0" border="0">
    <tr>
        <td colspan="2">
            {email-tag tag="p"}
                Bonjour, {$user['name']}.<br />
                {$Conf.ENV.user.name} veut comparer l'objectif:
            {/email-tag}
        </td>
    </tr>
    <tr>
        <td width="60" align="center">
            <img src="{$host}{if $Conf->ENV->user->thumbnail}{@userpics}{$Conf.ENV.user.thumbnail}{else}{@img}site/no-thumbnail.png{end}" width="50" height="50" />
        </td>
        <td>
            {email-tag tag="p"}"{$data['goal']}"{/email-tag}
        </td>
    </tr>
    <tr>
        <td colspan="2">
            {email-tag tag="p"}Confirmer ou rejeter la demande: <a href="{$host}{@home}users/{$user['login']}/compare/" target="_blank">{$host}{@home}users/{$user['login']}/compare/</a>{/email-tag}
            {email-tag tag="p"}- L'équipe 42goals{/email-tag}
        </td>
    </tr>
</table>