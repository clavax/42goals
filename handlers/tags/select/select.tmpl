<select {foreach $params as $key => $val}{$key}="{$val}"{end}>
{foreach $options as $index => $title}
    <option value="{$index}"{if (string)$index == (string)$value} selected="selected"{end}>{$title}</option>
{end}
</select>